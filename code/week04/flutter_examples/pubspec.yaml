name: week04_flutter_examples
description: Week 4 Flutter examples - the Hello World app built step by step, each step runnable on its own.
publish_to: 'none'
version: 1.0.0

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
