# Code examples

One folder per week: `week01/`, `week02/`, ...

Each week folder holds the examples shown in that week's lecture slides and lecture notes
(the PDFs are in `../lectures/`).

Run a Dart file with the Dart SDK (it comes with Flutter):

```
dart run <file>.dart
```
